#compdef ecdsautil

_ecdsautil() {
  local state

  _arguments \
    '1: :->option'\
    '*: :->file'

  case $state in
    (option) _arguments '1:options:(generate-key show-key sign verify)' ;;
      (file) _arguments '*:file:_files' ;;
  esac
}

_ecdsautil "$@"
